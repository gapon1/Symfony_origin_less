{% extends 'base.html.twig' %}



{% block body %}
    <h1>the genus:  {{ name }}</h1>

    <br/>
    <p>{{ dump() }}</p>
    <ul>
        {% for note in notes %}
            <li>{{ note }}</li>
        {% endfor %}
    </ul>
{% endblock  %}
